# Getting Started

- rename `.env-template` to `.env`
- fill the environment with yours!
  ```sh
  EMAIL_USERNAME=
  EMAIL_PASSWORD=
  EMAIL_HOST=
  EMAIL_PORT=
  ```
- go !
  ```sh
  yarn && yarn start
  ```
- endpoint
  - `/email`
  - `/verify`
